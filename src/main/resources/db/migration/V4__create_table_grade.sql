CREATE TABLE grades (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
   grade DOUBLE PRECISION NOT NULL,
   student_id BIGINT,
   subject_id BIGINT,
   teacher_id BIGINT,
   CONSTRAINT pk_grades PRIMARY KEY (id)
);

ALTER TABLE grades ADD CONSTRAINT FK_GRADES_ON_STUDENT FOREIGN KEY (student_id) REFERENCES students (id);

ALTER TABLE grades ADD CONSTRAINT FK_GRADES_ON_SUBJECT FOREIGN KEY (subject_id) REFERENCES subjects (id);

ALTER TABLE grades ADD CONSTRAINT FK_GRADES_ON_TEACHER FOREIGN KEY (teacher_id) REFERENCES teachers (id);