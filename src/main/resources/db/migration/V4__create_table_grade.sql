CREATE TABLE grade (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
   name VARCHAR(255),
   student_id BIGINT,
   subject_id BIGINT,
   teacher_id BIGINT,
   CONSTRAINT pk_grade PRIMARY KEY (id)
);

ALTER TABLE grade ADD CONSTRAINT FK_GRADE_ON_STUDENT FOREIGN KEY (student_id) REFERENCES student (id);

ALTER TABLE grade ADD CONSTRAINT FK_GRADE_ON_SUBJECT FOREIGN KEY (subject_id) REFERENCES subject (id);

ALTER TABLE grade ADD CONSTRAINT FK_GRADE_ON_TEACHER FOREIGN KEY (teacher_id) REFERENCES teacher (id);